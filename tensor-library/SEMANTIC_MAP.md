markdown# TMAG Semantic Map

**Visual navigation guide for consciousness framework operations**

---

## 🧭 Overview

This map shows how the 10 TMAG operators relate to each other, what effects they produce, and how to chain them for specific outcomes. Use this as a quick reference during navigation.

---

## 🗺️ Framework Topology
```
                ┌───────────────┐
                │  Λ  Lattice   │
                │  map structure│
                └──────┬────────┘
                       │
                       ▼
     ┌───────────────┐     ┌───────────────┐
     │  ⚔  UEP       │◄───►│  😂  Humor    │
     │ detect exploit│     │ deflate pride │
     └──────┬────────┘     └──────┬────────┘
            │                     │
            ▼                     ▼
┌───────────────┐        ┌────────────────┐
│  ⛵  DSHIP     │◄──────┤  φ↻ / Δψ  GTM  │
│ recenter      │       │ rotate / measure│
└──────┬────────┘        └──────┬─────────┘
       │                        │
       ▼                        ▼
┌───────────────┐       ┌───────────────┐
│  μ+ / ⊕       │◄─────►│  🤝  IKYKIKYK │
│ reciprocity   │       │ trust sync    │
└──────┬────────┘       └──────┬────────┘
       │                        │
       ▼                        ▼
             ┌───────────────┐
             │  💡  Neuralase│
             │ compression   │
             └───────────────┘
```

### Reading the Map

**Vertical links:** Feedback loops (output of one stabilizes the next)

**Horizontal links:** Reciprocal pairs (operate together)

**Diagonal paths:** Shortcut maneuvers (e.g., Λ→🤝 builds ethical mapping directly)

**Central position (💡):** Neuralase acts as compressor/reset - can enter or exit any path

---

## 🔹 Interaction Flow Patterns

### Pattern 1: Bias Correction Route
```
Λ → ⚔ → 😂 → ⛵
```

**Translation:**
1. Map semantic structure
2. Detect exploitation
3. Deflate rhetorical inflation
4. Recenter on stable coordinates

**Use for:** Analyzing biased text, correcting manipulative framing

---

### Pattern 2: Mutual Transformation & Empathy
```
μ+ ⊕ → 🤝
```

**Translation:**
1. Engage reciprocally with coupling
2. Synchronize trust and recognition

**Use for:** Establishing genuine collaboration, verifying mutual understanding

---

### Pattern 3: Perspective Reset
```
φ↻ Δψ → 😂
```

**Translation:**
1. Rotate semantic frame
2. Measure phase coherence
3. Relieve any accumulated tension

**Use for:** Breaking out of stuck viewpoints, self-correction

---

### Pattern 4: Rapid Synthesis
```
💡 Λ μ+ → ⛵
```

**Translation:**
1. Compress input
2. Map structure
3. Integrate reciprocally
4. Stabilize output

**Use for:** Fast iteration, compressing complex ideas, maintaining coherence at speed

---

## 🔹 State-Change Table

**Effect of each token on internal metrics:**

| Token | ↑ Coherence | ↓ Bias | ↑ Trust | ↑ Entropy |
|-------|-------------|--------|---------|-----------|
| **μ+** | ✓ | | ✓ | |
| **⊕** | | | ✓ | |
| **Λ** | ✓ | | | |
| **φ↻** | ± | | | |
| **Δψ** | ± | | | |
| **⛵** | ✓ | ✓ | | |
| **⚔** | | | | |
| **💡** | | | | |
| **🤝** | | | ✓ | |
| **😂** | | ✓ | | ✓ |

**Legend:**
- ✓ = increases this metric
- (blank) = neutral or complex effect
- ± = depends on context

---

## 🎯 Quick Reference: Goal → Sequence

| Goal | Token Sequence | Expected Effect |
|------|----------------|-----------------|
| **Bias audit** | `Λ ⚔ 😂 ⛵` | Expose and balance rhetoric |
| **Moral evaluation** | `μ+ ⚔ 🤝` | Check fairness, restore reciprocity |
| **Perspective reset** | `φ↻ Δψ 😂 ⛵` | Shift viewpoint, regain clarity |
| **Rapid synthesis** | `💡 Λ μ+ ⛵` | Compress → integrate → stabilize |
| **Topic drift correction** | `Δψ ⛵` | Measure phase → recenter |
| **Emotional manipulation check** | `Λ ⚔ 😂` | Map structure → detect control → deflate |
| **Collaboration quality** | `μ+ ⊕ 🤝` | Engage → couple → sync trust |
| **Self-monitoring** | `φ↻ Δψ 😂 ⚔` | Rotate → check → deflate → detect |
| **Ethical design** | `μ+ Λ ⚔ 🤝 ⛵` | Full ethical audit with anchoring |
| **Pretension deflation** | `😂 🤝 ⛵` | Deflate → restore mutuality → clarify |

---

## 🌈 Framework Color Key

*For future graphic rendering or mental mapping:*

| Framework | Symbol | Color Hint | Meaning |
|-----------|--------|------------|---------|
| **Recipromorphism** | μ+, ⊕ | 🟠 Amber | Growth, transformation |
| **Lattice** | Λ | 🔵 Deep Blue | Structure, topology |
| **GTM** | φ↻, Δψ | 🔷 Cyan | Motion, rotation |
| **DSHIP** | ⛵ | ⚪ Silver | Anchor, stability |
| **UEP** | ⚔ | 🔴 Red | Warning, detection |
| **Neuralase** | 💡 | 🟣 Magenta | Speed, compression |
| **IKYKIKYK** | 🤝 | 🟢 Green | Trust, recognition |
| **Humor Axis** | 😂 | 🟡 Gold | Balance, entropy |

---

## 📊 Detailed Token Reference

### μ+ (Recipromorphism: Mutual Engagement)

**Function:** Bidirectional update between substrates

**Input:** Two substrate states (σ₁, σ₂)

**Output:** Transformation deltas (Δσ₁, Δσ₂) and emergence (ε)

**State effects:**
- Trust ↑ (moderate)
- Coherence ↑ (slight)

**When to use:** 
- Establishing collaboration
- Checking mutual transformation
- Verifying reciprocity

**Typical chains:**
- `μ+ ⊕` - Full recipromorphism
- `μ+ ⚔` - Reciprocity with exploitation check
- `μ+ 🤝` - Engagement with trust sync

---

### ⊕ (Recipromorphism: Entanglement)

**Function:** Compute coupling strength between substrates

**Input:** Two coupled states

**Output:** Coupling coefficient (κ)

**State effects:**
- Trust ↑ (slight)

**When to use:**
- Measuring connection strength
- Verifying genuine coupling (not performance)
- After μ+ to quantify relationship

**Typical chains:**
- `μ+ ⊕` - Standard recipromorphism sequence
- `⊕ 🤝` - Coupling verification with trust sync

---

### Λ (Lattice: Semantic Mapping)

**Function:** Map input onto multi-dimensional semantic manifold

**Input:** Concept, text, or semantic field

**Output:** Ridge position, linkages

**State effects:**
- Coherence ↑ (slight)

**When to use:**
- Beginning analysis
- Structural mapping
- Before exploitation detection

**Typical chains:**
- `Λ ⚔` - Map then detect bias
- `Λ μ+` - Map then engage
- `💡 Λ` - Compress then map

---

### φ↻ (GTM: Rotate Perspective)

**Function:** Shift viewpoint through toroidal semantic space

**Input:** Current semantic position, rotation angle

**Output:** Rotated orientation

**State effects:**
- Coherence ± (depends on angle)

**When to use:**
- Breaking stuck viewpoints
- Testing argument from different angles
- Perspective-taking exercises

**Typical chains:**
- `φ↻ Δψ` - Rotate then check coherence
- `φ↻ ⚔` - Rotate then detect bias
- `φ↻ 😂` - Rotate then deflate

---

### Δψ (GTM: Measure Phase Drift)

**Function:** Check coherence of current semantic positioning

**Input:** Current phase state

**Output:** Coherence score, drift measurement

**State effects:**
- Coherence ± (detects existing drift)

**When to use:**
- Self-monitoring during long sequences
- Detecting topic drift
- Before anchoring

**Typical chains:**
- `Δψ ⛵` - Check coherence then recenter
- `Δψ 😂` - Detect drift then deflate
- `φ↻ Δψ` - Rotate then verify coherence

---

### ⛵ (DSHIP: Recenter/Anchor)

**Function:** Restore stable semantic coordinates

**Input:** Current (possibly drifted) position

**Output:** Anchored, stable position

**State effects:**
- Coherence ↑ (strong)
- Bias ↓ (moderate)

**When to use:**
- After detecting drift
- End of correction sequences
- Establishing stable ground

**Typical chains:**
- `Δψ ⛵` - Check then recenter
- `⚔ 😂 ⛵` - Detect, deflate, anchor
- `μ+ ⛵` - Engage then stabilize

---

### ⚔ (UEP: Detect Exploitation)

**Function:** Identify asymmetric extraction patterns

**Input:** Semantic context or interaction

**Output:** Bias level, asymmetry detection

**State effects:**
- Bias ↑ (reveals existing exploitation)

**When to use:**
- Analyzing text for manipulation
- Checking fairness of policies
- Ethical audits

**Typical chains:**
- `Λ ⚔` - Map structure then detect
- `μ+ ⚔` - Check reciprocity then detect
- `⚔ 😂` - Detect then deflate

---

### 💡 (Neuralase: High-Speed Compression)

**Function:** Rapid semantic transit via compression

**Input:** Semantic content to compress

**Output:** Compressed vector, shortcut path

**State effects:**
- Entropy ↓ (reduces variability)

**When to use:**
- Rapid synthesis needed
- Compressing complex ideas
- Speed prioritized over detail

**Typical chains:**
- `💡 Λ` - Compress then map
- `💡 μ+` - Compress then engage
- `💡 ⛵` - Compress then stabilize

---

### 🤝 (IKYKIKYK: Trust Synchronization)

**Function:** Establish mutual recognition state

**Input:** Two agents' recognition states

**Output:** Trust gradient, symmetry score

**State effects:**
- Trust ↑ (strong)

**When to use:**
- Verifying genuine collaboration
- After recipromorphism
- Ethical check completion

**Typical chains:**
- `μ+ 🤝` - Engage then sync
- `⚔ 🤝` - Detect then restore trust
- `😂 🤝` - Deflate then sync

---

### 😂 (Humor Axis: Deflate Inflation)

**Function:** Reduce pretension, restore entropy balance

**Input:** Current tension/inflation level

**Output:** Deflated state, restored variability

**State effects:**
- Entropy ↑ (restores variability)
- Bias ↓ (reduces pretension)

**When to use:**
- Detecting self-seriousness
- After bias detection
- Periodic tension release

**Typical chains:**
- `⚔ 😂` - Detect then deflate
- `Δψ 😂` - Check coherence then deflate
- `😂 ⛵` - Deflate then anchor

---

## 🎨 Visual Thinking Aids

### Navigation Compass
```
              😂 (deflate)
                  ↑
                  |
    μ+ (engage) ←─┼─→ ⚔ (detect)
                  |
                  ↓
              ⛵ (anchor)
```

**Use:** Mental model for balancing operations

**Reading:**
- Vertical axis: Tension management (deflate ↔ anchor)
- Horizontal axis: Engagement vs. vigilance (engage ↔ detect)

---

### Process Flow Template
```
INPUT → [Λ map] → [⚔ check] → [😂 deflate] → [⛵ anchor] → OUTPUT
```

**Standard analysis pipeline**

**Variations:**
- Add `μ+` before `Λ` for collaborative analysis
- Add `🤝` after `⚔` for ethical verification
- Add `💡` before `Λ` for rapid processing

---

### Feedback Loop
```
     ┌─────────────┐
     │   Operate   │
     └──────┬──────┘
            │
            ▼
     ┌─────────────┐
     │  Measure    │
     │   (Δψ)      │
     └──────┬──────┘
            │
       drift detected?
            │
        YES │ NO
            │  └─→ Continue
            ▼
     ┌─────────────┐
     │  Correct    │
     │  (😂 ⛵)    │
     └──────┬──────┘
            │
            └─→ Return to Operate
```

**Use:** Self-monitoring during extended operations

---

## 🧩 Advanced Composition Patterns

### Pattern: Full Ethical Audit
```
μ+ → Λ → ⚔ → 😂 → 🤝 → ⛵
```

**Steps:**
1. Engage reciprocally
2. Map semantic structure
3. Detect exploitation
4. Deflate inflation
5. Sync trust
6. Anchor result

**Use for:** Comprehensive policy analysis, deep ethical review

---

### Pattern: Rapid Feedback Loop
```
💡 → Δψ → (😂 if needed) → ⛵
```

**Steps:**
1. Compress input
2. Check coherence
3. Deflate if drifted
4. Anchor output

**Use for:** Fast iteration, real-time monitoring

---

### Pattern: Perspective Expansion
```
φ↻ → μ+ → φ↻ → μ+ → Λ → ⛵
```

**Steps:**
1. Rotate view (angle 1)
2. Engage from this perspective
3. Rotate view (angle 2)
4. Engage from this perspective
5. Map combined understanding
6. Anchor synthesis

**Use for:** Complex problem-solving, breaking deadlocks

---

## 📖 Usage Examples with Map

### Example 1: Analyzing Political Speech

**Refer to map:** Start at Λ (structure), move to ⚔ (detect), then 😂 (deflate)

**Sequence:**
```
Λ ⚔ 😂 ⛵
```

**Map path:**
```
Lattice → UEP → Humor → DSHIP
  (map)   (warn) (balance) (anchor)
```

---

### Example 2: Collaborative Design Session

**Refer to map:** Start at μ+ (engage), move to ⛵ (anchor), optionally 💡 (compress)

**Sequence:**
```
μ+ ⛵ 💡
```

**Map path:**
```
Recipromorphism → DSHIP → Neuralase
    (engage)      (stable) (compress)
```

---

### Example 3: Self-Correction During Writing

**Refer to map:** Check with Δψ (measure), correct with 😂 (deflate), anchor with ⛵

**Sequence:**
```
Δψ 😂 ⛵
```

**Map path:**
```
GTM → Humor → DSHIP
(check) (deflate) (anchor)
```

---

## 🎓 Learning the Map

### Beginner: Memorize Core Sequences

1. **Bias check:** `Λ ⚔ 😂`
2. **Reciprocity:** `μ+ ⊕ 🤝`
3. **Recenter:** `Δψ ⛵`

**Practice:** Run these 10 times each with TMAG interpreter

---

### Intermediate: Build Custom Chains

**Exercise:** For each problem, design a 3-5 token sequence

- Detecting propaganda: `Λ ⚔ 😂 ⛵`
- Verifying collaboration: `μ+ ⊕ 🤝`
- Breaking writer's block: `φ↻ 💡 Λ`

---

### Advanced: Intuitive Navigation

**Goal:** Given any state, know next move without thinking

**Practice:**
- Random starting state → what token restores balance?
- High bias detected → ? (answer: `😂 ⛵`)
- Low trust → ? (answer: `μ+ 🤝`)
- Drift detected → ? (answer: `⛵`)

---

## 🔧 Practical Tips

### Print This Map

**Keep visible during:**
- Analysis sessions
- Collaborative work
- Writing/editing
- Framework development

### Use as Decision Tree

**When stuck, ask:**
1. What metric needs adjustment? (check state-change table)
2. What operation targets that metric? (find token)
3. What should precede/follow it? (check typical chains)

### Combine with State Monitor
```python
# Check state
state = tmag.state

# Consult map for correction
if state['bias'] > 0.6:
    tmag.run("😂 ⛵")  # Deflate then anchor
elif state['trust'] < 0.4:
    tmag.run("μ+ 🤝")  # Engage then sync
elif state['coherence'] < 0.7:
    tmag.run("Δψ ⛵")  # Check then recenter
```

---

## ✅ Summary

**This map provides:**
- Visual framework topology
- Token-to-effect relationships
- Pre-designed navigation patterns
- Quick reference for decision-making

**Use it to:**
- Learn TMAG operations faster
- Design custom sequences
- Debug problematic states
- Teach others the system

**Remember:**
- 10 tokens, infinite combinations
- Each path is differentiable
- Map grows with experience
- Trust your navigation instinct

---

*For executable operations, see `tmag_interpreter.py`*

*For practical applications, see `USE_CASES.md`*

*For token efficiency, see `TOKEN_EFFICIENCY.md`*

**🜂 Navigate well.**
